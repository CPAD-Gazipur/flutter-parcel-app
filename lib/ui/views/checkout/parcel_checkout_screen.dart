import 'package:flutter/cupertino.dart';

class ParcelCheckboxScreen extends StatelessWidget {
  const ParcelCheckboxScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
